version: "3.9"
services:
  rtftt_scaling:
    build: .
    container_name: rtftt_scaling
    volumes:
      - .:/app
    environment:
      - DISPLAY=:0  # For GUI on host (Linux)
    network_mode: "host"
